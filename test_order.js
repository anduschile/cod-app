require('dotenv').config({path: '.env.local'}); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const { data, error } = await supabase.from('codpi_orders').select('id, product_id, estado, precio_venta_unidad, cantidad, costo_producto_unidad, costo_envio, costo_recaudo, codpi_products(id, nombre, costo_producto, costo_envio_estimado, costo_recaudo_estimado, comision_pasarela, costo_embalaje)').eq('precio_venta_unidad', 7900).limit(2); console.log(JSON.stringify(data, null, 2)); } run();
